[project]
name = "minimal-reproduction-template"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.2",
]

[project.optional-dependencies]
test = [
    "pytest>=8.3.4",
]
